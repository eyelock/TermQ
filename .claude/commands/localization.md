# /localization — Localization Management

Invoke the localizer agent with the specified action.

```
/localization <action> [language]
```

**Actions:** `extract` | `translate <lang>` | `validate [lang]` | `status` | `audit <lang>` | `add-string <key>` | `sync`
