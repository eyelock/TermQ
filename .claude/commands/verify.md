# /verify — Quality Verification

Invoke the reviewer agent on all changed files.

If any `Sources/TermQ/` UI files changed, also invoke the localizer agent with action: validate.

All findings must be resolved before proceeding to /commit.
