# /commit — Create a Local Commit

Stage changed files and create a local commit after /verify has passed.

Use the commit-conventions skill for branch naming, message format, and the Co-Authored-By line.
